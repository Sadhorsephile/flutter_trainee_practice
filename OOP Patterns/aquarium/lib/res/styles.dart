import 'package:flutter/material.dart';

/// Стили, используемые в приложении
class AppStyles {
  static const appbarTextStyle = TextStyle(color: Colors.black);
  static const userLogStyle = TextStyle(color: Colors.white);
}
